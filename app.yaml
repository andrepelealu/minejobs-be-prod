runtime: php  # language of the app
env: flex     # let app engine know we use flexible environment
runtime_config:
  document_root: public   #folder where index.php is
# Ensure we skip ".env", which is only for local development
skip_files:
 - .env #we want to skip this to make sure we don’t mess stuff up on the server
env_variables:
 # Put production environment variables here.
 APP_ENV: prodiction   # or production
 APP_DEBUG : false # or false 
 APP_KEY: base64:AqwJXE2xGwqmMagncqrLe11a6KB+Z8SA6hWAziy57BI=
#go to generate app key paragraf in this tutorial
 CACHE_DRIVER: database
# instead of putting the cache in the database I recommend using redis
 SESSION_DRIVER: database #or file since both work
 APP_LOG: daily
 APP_TIMEZONE: UTC #your timezone of choice
# follow the part of the tutorial on setting up your SQL database
 DB_CONNECTION: mysql
 DB_HOST: localhost
 DB_DATABASE: minejobs
 DB_USERNAME: minejobs
 DB_PASSWORD: 17agustus
 DB_SOCKET: /cloudsql/minejobs-292110:asia-southeast2:minejobs
 QUEUE_DRIVER: database #in case you execute queued jobs
 MAIL_DRIVER: smtp
 MAIL_HOST: smtp.mailtrap.io
 MAIL_PORT: 2525
 MAIL_USERNAME: 64e8af7037c947
 MAIL_PASSWORD: 58923a41a9f30b
 MAIL_ENCRYPTION: null
 GOOGLE_ID: 267474974587-raddoaqb1ldjvop4iqmtjqkiuibmld1p.apps.googleusercontent.com
 GOOGLE_SECRET: X1dapUIUR0FWhHs50eeDIdEI
 GOOGLE_URL: http://api.minejobs.com/api/auth/google/callback
 JWT_SECRET: rXM6tFJ10ePmZSfv3nWchVZ5yeB913djXGyH3965avedPWFiQjMI0LUow7pxG0Vm
 LOG_DELETE:  true # this is a parameter added by us in the project .env file. You can add here any setting you would add to your .env file
 GOOGLE_VISION_PROJECT_ID : minejobs-292110
#we need this for the flex environment
beta_settings:
   # for Cloud SQL, set this value to the Cloud SQL connection name,
   # e.g. "project:region:cloudsql-instance"
   cloud_sql_instances: minejobs-292110:asia-southeast2:minejobs